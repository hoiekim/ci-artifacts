# CI screenshots

CI-captured UI screenshots for one or more projects, each
namespaced by its own prefix, PR, and run:
`<project>/pr-<number>/<run_id>/*.png` -- turnip-ios's own are
under `turnip-ios/`. Written by automation; safe to prune old
entries.
